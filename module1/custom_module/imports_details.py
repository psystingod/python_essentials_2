import sys;

for p in sys.path:
    print(p);

# creamos modulos con un simple archivo .py que contenga clases, funciones, variables o constantes.
# un paquete es una estructura mas compleja de modulos y subpaquetes, los importamos con la 
# notacion paquete.subpaquete.modulo sin la extension.py.

# la comunidad mas grande de paquetes (repositorio) es PYPI